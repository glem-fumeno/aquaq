[project]
name = "aquaq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Emilia Glibowska", email = "emilia.glibowska@elmark.com.pl" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
aquaq = "aquaq:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
